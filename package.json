{
  "name": "@pickme/reminder",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "vite --host",
    "build": "vite build",
    "build:single-spa": "vite build --mode single-spa",
    "preview": "vite preview --host",
    "preview:single-spa": "vite preview --mode single-spa --host",
    "start": "npm run build && npm run build:single-spa && npm run preview:single-spa"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@vitejs/plugin-basic-ssl": "^1.2.0",
    "vite": "^6.0.11",
    "vite-plugin-static-copy": "^2.2.0"
  },
  "dependencies": {
    "jwt-decode": "^4.0.0",
    "vite-plugin-single-spa": "^0.9.0"
  }
}
